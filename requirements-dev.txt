# Everything in the runtime image, plus data processing, training and evaluation.
# Install this one for local development:  pip install -r requirements-dev.txt
-r requirements.txt

pandas==2.2.2
scikit-learn==1.9.0
datasets>=3.0
accelerate>=1.0
evaluate>=0.4

# Only needed by the legacy (2020) analysis scripts under Code/pkg/analysis and
# Code/pkg/datasets/*/processing_scripts/data_summary.py. Not used by the app.
# spaCy also needs:  python -m spacy download en_core_web_md
spacy==3.8.15
wordcloud>=1.9
